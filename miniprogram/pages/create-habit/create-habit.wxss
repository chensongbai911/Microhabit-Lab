/* 优化提醒区域为垂直布局 */
.reminder-preview-vertical {
  background: linear-gradient(135deg, #07C160 0%, #2AD47D 100%);
  border-radius: 12rpx;
  padding: 24rpx 28rpx;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  color: #FFFFFF;
  box-shadow: 0 4rpx 12rpx rgba(7, 193, 96, 0.2);
  animation: slideInUp 0.3s ease-out;
}
.reminder-main-wrapper {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  width: 100%;
}
.reminder-main {
  display: flex;
  flex-direction: row;
  align-items: center;
  width: 100%;
}
.reminder-icon {
  font-size: 40rpx;
  flex-shrink: 0;
  line-height: 1;
}
.reminder-info {
  margin-left: 16rpx;
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8rpx;
  flex: 1;
}
.reminder-label {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.95);
  line-height: 1.4;
}
.reminder-time {
  font-weight: 700;
  color: #FFF500;
  font-size: 36rpx;
  line-height: 1;
}
/* 可编辑时间样式 */
.reminder-time-editable {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 16rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 12rpx;
  border: 2rpx dashed rgba(255, 245, 0, 0.6);
  transition: all 0.3s ease;
  position: relative;
}
.reminder-time-editable::after {
  content: '';
  position: absolute;
  inset: -4rpx;
  border-radius: 14rpx;
  padding: 2rpx;
  background: linear-gradient(45deg, transparent, rgba(255, 245, 0, 0.3), transparent);
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  animation: shimmer 2s linear infinite;
}
@keyframes shimmer {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}
.reminder-time-editable:active {
  background: rgba(255, 255, 255, 0.3);
  transform: scale(0.97);
}
.edit-icon {
  font-size: 24rpx;
  opacity: 0.95;
}
/* 时间编辑提示 */
.time-edit-hint {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 16rpx 8rpx 56rpx;
  background: rgba(255, 245, 0, 0.15);
  border-radius: 8rpx;
  animation: bounce 2s ease-in-out infinite;
}
@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-4rpx); }
}
.time-edit-hint .hint-icon {
  font-size: 24rpx;
}
.time-edit-hint .hint-text {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.95);
  font-weight: 500;
}
picker {
  display: inline;
}
.reminder-tip-vertical {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.75);
  line-height: 1.4;
  padding-left: 56rpx;
  word-break: break-all;
  max-width: 100%;
}
/* 新建习惯页样�?*/

.container {
  min-height: 100vh;
  background: #F5F5F5;
}

.page-container {
  padding: 32rpx;
  min-height: 100vh;
}

/* 头部 */
.header {
  margin-bottom: 48rpx;
  padding: 24rpx 0;
}

.title {
  font-size: 48rpx;
  font-weight: 700;
  color: #000000;
  margin-bottom: 16rpx;
  line-height: 1.2;
}

.subtitle {
  font-size: 28rpx;
  color: #999999;
  line-height: 1.5;
}

/* Phase 1改进: 编辑模式的进度显�?*/
.habit-status {
  display: flex;
  gap: 16rpx;
  margin-top: 16rpx;
}

.status-item {
  flex: 1;
  background: #FFFFFF;
  border-radius: 12rpx;
  padding: 12rpx 16rpx;
  text-align: center;
  border: 1rpx solid #EDEDED;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.status-label {
  display: block;
  font-size: 22rpx;
  color: #999999;
  margin-bottom: 4rpx;
}

.status-value {
  display: block;
  font-size: 28rpx;
  font-weight: 700;
  color: #07C160;
}

/* 表单 */
.form {
  margin-bottom: 180rpx;
  padding-bottom: 40rpx;
}

.form-item {
  margin-bottom: 48rpx;
  padding: 28rpx;
  background: #FFFFFF;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.04);
  border: 1rpx solid #EDEDED;
}

.form-label {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 20rpx;
  line-height: 1.2;
}

/* P1-2: 表单副标题样�?*/
.form-sublabel {
  font-size: 24rpx;
  color: var(--text-tertiary);
  margin-bottom: 16rpx;
  line-height: 1.4;
}

.form-input {
  width: 100%;
  height: 80rpx;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 16rpx;
  padding: 0 24rpx;
  font-size: 30rpx;
  color: var(--text-primary);
  border: 2rpx solid rgba(102, 126, 234, 0.15);
  transition: all 0.3s ease;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.form-input:focus {
  border-color: #07C160;
  background: white;
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.2);
}

.form-hint {
  margin-top: 12rpx;
  font-size: 24rpx;
  line-height: 1.6;
  color: var(--text-tertiary);
}

.char-count {
  color: var(--text-tertiary);
  margin-left: 16rpx;
  flex-shrink: 0;
}

/* 触发器选项 */
.trigger-options {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.trigger-btn {
  padding: 18rpx 32rpx;
  background: rgba(255, 255, 255, 0.7);
  border: 2rpx solid rgba(102, 126, 234, 0.2);
  border-radius: 999rpx;
  font-size: 28rpx;
  color: var(--text-secondary);
  transition: all 0.3s ease;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.trigger-btn.active {
  background: linear-gradient(135deg, #07C160 0%, #059048 100%);
  border-color: transparent;
  color: white;
  box-shadow: 0 6rpx 20rpx rgba(102, 126, 234, 0.35);
  transform: translateY(-2rpx);
}

.trigger-btn:active {
  transform: scale(0.95);
}

.form-item .mt-sm {
  margin-top: 24rpx;
}

/* 提醒时间预览 */
.reminder-preview {
  background: linear-gradient(135deg, #07C160 0%, #2AD47D 100%);
  border-radius: 12rpx;
  padding: 20rpx 24rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
  color: #FFFFFF;
  box-shadow: 0 4rpx 12rpx rgba(7, 193, 96, 0.2);
  animation: slideInUp 0.3s ease-out;
}

.reminder-icon {
  font-size: 40rpx;
  flex-shrink: 0;
  line-height: 1;
}

.reminder-info {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 4rpx;
  flex-wrap: wrap;
}

.reminder-label {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.95);
}

.reminder-time {
  font-size: 32rpx;
  font-weight: 700;
  color: #FFFFFF;
}

.reminder-tip {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.85);
  margin-top: 8rpx;
}

.reminder-tip .tip-text {
  display: block;
  line-height: 1.4;
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(10rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 频次区域整体 */
.frequency-section {
  margin-bottom: 32rpx !important;
  padding: 32rpx 28rpx !important;
}

/* 频次选项 */
.frequency-options {
  display: flex;
  gap: 16rpx;
  margin-bottom: 0;
  margin-top: 12rpx;
}

.frequency-btn {
  flex: 1;
  padding: 24rpx 12rpx;
  background: rgba(255, 255, 255, 0.7);
  border: 2rpx solid rgba(102, 126, 234, 0.2);
  border-radius: 16rpx;
  text-align: center;
  transition: all 0.3s ease;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.frequency-btn.active {
  background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
  border-color: #10b981;
  box-shadow: 0 4rpx 16rpx rgba(16, 185, 129, 0.2);
  transform: translateY(-2rpx);
}

.frequency-main {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 4rpx;
  line-height: 1.2;
}

.frequency-btn.active .frequency-main {
  color: #047857;
}

.frequency-sub {
  font-size: 22rpx;
  color: var(--text-tertiary);
  line-height: 1.2;
  margin-top: 4rpx;
}

.frequency-btn.active .frequency-sub {
  color: #059669;
}

/* P0-1: 频次警告样式 */
.frequency-warning {
  margin-top: 20rpx;
  padding: 16rpx 18rpx;
  background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
  border: 1rpx solid #fcd34d;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  gap: 10rpx;
  animation: shake 0.5s ease;
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4rpx); }
  75% { transform: translateX(4rpx); }
}

.frequency-warning .warning-icon {
  font-size: 26rpx;
  line-height: 1;
  flex-shrink: 0;
}

.frequency-warning .warning-text {
  font-size: 24rpx;
  color: #92400e;
  font-weight: 500;
  line-height: 1.4;
}

/* Phase 2新增: 频次影响说明 */
.frequency-impact {
  margin-top: 20rpx;
  padding: 20rpx 22rpx;
  background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
  border-radius: 12rpx;
  border-left: 4rpx solid #10b981;
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.impact-content {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

/* P1-1: 温暖的完成率文案样式 */
.impact-label-new {
  font-size: 26rpx;
  font-weight: 600;
  color: #047857;
  line-height: 1.4;
}

.impact-tips {
  font-size: 22rpx;
  color: #059669;
  line-height: 1.5;
  opacity: 0.85;
}
/* 底部操作按钮 */
.footer-actions {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(30rpx);
  border-top: 1rpx solid rgba(102, 126, 234, 0.12);
  display: flex;
  gap: 16rpx;
  z-index: 100;
  box-shadow: 0 -8rpx 32rpx rgba(0, 0, 0, 0.06);
}

.footer-actions .btn {
  flex: 1;
  height: 92rpx;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 20rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.footer-actions .btn-outline {
  background: rgba(102, 126, 234, 0.08);
  border: 2rpx solid rgba(102, 126, 234, 0.2);
  color: #07C160;
}

.footer-actions .btn-outline:active {
  background: rgba(102, 126, 234, 0.15);
  transform: scale(0.97);
}

.footer-actions .btn-primary {
  background: linear-gradient(135deg, #07C160 0%, #059048 100%);
  box-shadow: 0 6rpx 20rpx rgba(102, 126, 234, 0.35);
  color: #FFFFFF !important;
}

.footer-actions .btn-primary:active {
  transform: scale(0.97);
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.3);
}

.footer-actions .btn-primary:disabled {
  background: rgba(102, 126, 234, 0.3);
  box-shadow: none;
  color: #FFFFFF !important;
}

/* P0-2: 底部安抚提示 - 优化位置和样式 */
.footer-hint {
  position: fixed;
  bottom: 140rpx;
  left: 50%;
  transform: translateX(-50%);
  text-align: center;
  z-index: 99;
  animation: fadeInUp 0.4s ease;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateX(-50%) translateY(10rpx);
  }
  to {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
}

.footer-hint .hint-text {
  font-size: 24rpx;
  color: #059669;
  font-weight: 500;
  background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
  padding: 12rpx 28rpx;
  border-radius: 999rpx;
  display: inline-block;
  border: 1rpx solid #a7f3d0;
  box-shadow: 0 4rpx 16rpx rgba(16, 185, 129, 0.15);
}

/* Phase 2新增: 推荐提示 */
.recommendation-hint {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 16rpx;
  padding: 14rpx 18rpx;
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.08) 0%, rgba(118, 75, 162, 0.05) 100%);
  border-radius: 12rpx;
  border: 1rpx solid rgba(102, 126, 234, 0.15);
  box-shadow: 0 2rpx 8rpx rgba(102, 126, 234, 0.08);
}

.hint-icon {
  font-size: 28rpx;
  min-width: 28rpx;
  line-height: 1;
}

.hint-text {
  font-size: 24rpx;
  color: #5865c7;
  line-height: 1.5;
}

.hint-text strong {
  font-weight: 700;
  color: #07C160;
}

/* Phase 2新增: 表单反馈 */
.form-feedback {
  display: inline-flex;
  align-items: center;
  gap: 12rpx;
  margin-top: 8rpx;
  padding: 6rpx 16rpx;
  background: rgba(102, 126, 234, 0.06);
  border-radius: 20rpx;
  font-size: 24rpx;
}

.char-count {
  color: var(--text-secondary);
  font-weight: 500;
}

.feedback-status {
  color: #07C160;
  font-weight: 600;
  font-size: 22rpx;
}

/* 触发器信息框 */
.trigger-info-box {
  display: flex;
  align-items: flex-start;
  gap: 8rpx;
  padding: 12rpx 16rpx;
  background-color: rgba(7, 193, 96, 0.08);
  border-radius: 8rpx;
  margin-bottom: 16rpx;
  border-left: 4rpx solid #07C160;
}

.trigger-info-box .info-icon {
  font-size: 24rpx;
  flex-shrink: 0;
  line-height: 1.4;
}

.trigger-info-box .info-text {
  font-size: 26rpx;
  color: #059048;
  line-height: 1.5;
}

/* Phase 2新增: 触发器分类选择 */
.trigger-categories {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.category-tag {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 12rpx 20rpx;
  background: rgba(255, 255, 255, 0.7);
  border-radius: 24rpx;
  border: 2rpx solid rgba(102, 126, 234, 0.15);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.03);
}

.category-tag:active {
  transform: scale(0.95);
}

.category-tag.active {
  background: linear-gradient(135deg, #07C160 0%, #059048 100%);
  border: 2rpx solid transparent;
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.3);
  transform: translateY(-2rpx);
}

.category-icon {
  font-size: 28rpx;
  line-height: 1;
  filter: grayscale(0.3);
  transition: filter 0.3s ease;
}

.category-tag.active .category-icon {
  filter: grayscale(0);
}

.category-name {
  font-size: 26rpx;
  color: var(--text-secondary);
  font-weight: 500;
  transition: color 0.3s ease;
}

.category-tag.active .category-name {
  color: #FFFFFF;
  font-weight: 600;
}

/* Phase 2新增: 触发器列�?*/
.trigger-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.trigger-option {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 20rpx 18rpx;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 16rpx;
  border: 2rpx solid rgba(102, 126, 234, 0.12);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.03);
}

.trigger-option:active {
  transform: scale(0.97);
}

.trigger-option.selected {
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.12) 0%, rgba(118, 75, 162, 0.08) 100%);
  border: 2rpx solid #07C160;
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.2);
  transform: translateY(-2rpx);
}

/* 自定义时机按钮特殊样式 */
.trigger-other {
  border-style: dashed;
  border-color: #FFA940;
  background: #FFF7E6;
  color: #FAAD14;
  font-weight: 600;
}

.trigger-other.selected {
  border-color: #FAAD14;
  background: #FFF1B8;
  color: #D48806;
}

.trigger-icon {
  font-size: 28rpx;
  min-width: 28rpx;
}

.trigger-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.trigger-label {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
}

.trigger-time {
  font-size: 20rpx;
  color: var(--text-tertiary);
  margin-top: 4rpx;
}

.trigger-checkmark {
  font-size: 24rpx;
  color: #07C160;
  font-weight: bold;
}

/* Phase 2新增: 触发器完成率数据 */
.trigger-stats {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2rpx;
  min-width: 60rpx;
  padding: 8rpx 12rpx;
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.08) 100%);
  border-radius: 12rpx;
}

.stat-completion {
  font-size: 28rpx;
  color: #07C160;
  font-weight: 700;
  line-height: 1;
}

.stat-label {
  font-size: 20rpx;
  color: #999;
  line-height: 1;
  margin-top: 4rpx;
}

/* Phase 2新增: 自定义触发器 */
.custom-trigger {
  margin-top: 24rpx;
  animation: slideInDown 0.3s ease-out;
}

.custom-trigger-hint {
  display: flex;
  align-items: flex-start;
  gap: 10rpx;
  padding: 16rpx 20rpx;
  background: linear-gradient(135deg, rgba(250, 173, 64, 0.08) 0%, rgba(255, 197, 61, 0.12) 100%);
  border-radius: 12rpx;
  margin-bottom: 16rpx;
  border-left: 4rpx solid #FAAD14;
}

.custom-trigger-hint .hint-icon {
  font-size: 26rpx;
  flex-shrink: 0;
  line-height: 1.2;
}

.custom-trigger-hint .hint-text {
  font-size: 26rpx;
  color: #D48806;
  line-height: 1.5;
  font-weight: 500;
}

.custom-trigger .form-input {
  padding: 16rpx 20rpx;
  height: 88rpx;
  border: 2rpx solid #FAAD14;
  border-radius: 12rpx;
  font-size: 28rpx;
  background: rgba(250, 173, 64, 0.04);
  transition: all 0.3s ease;
}

.custom-trigger .form-input:focus {
  border-color: #FAAD14;
  background: #FFF;
  box-shadow: 0 4rpx 16rpx rgba(250, 173, 64, 0.2);
}

@keyframes slideInDown {
  from {
    opacity: 0;
    transform: translateY(-10rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Phase 3新增: 影响分析警告 */
.impact-analysis {
  margin-top: 24rpx;
  padding: 16rpx;
  background-color: rgba(255, 255, 255, 0.95);
  border-radius: 8rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.08);
  animation: slideIn 0.3s ease-out;
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(-8rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.impact-header {
  margin-bottom: 12rpx;
}

.impact-severity {
  display: inline-flex;
  align-items: center;
  gap: 6rpx;
  padding: 6rpx 12rpx;
  border-radius: 6rpx;
  font-size: 24rpx;
  font-weight: 600;
  transition: all 0.2s ease;
}

.impact-severity.info {
  color: #4FC3F7;
  background-color: rgba(79, 195, 247, 0.1);
}

.impact-severity.warning {
  color: #FFA726;
  background-color: rgba(255, 167, 38, 0.1);
}

.impact-severity.danger {
  color: #EF5350;
  background-color: rgba(239, 83, 80, 0.1);
}

.impact-message {
  display: block;
  font-size: 26rpx;
  color: #333;
  line-height: 1.6;
  margin-bottom: 12rpx;
  font-weight: 500;
}

.impact-list {
  margin-bottom: 12rpx;
  padding-left: 8rpx;
}

.impact-item-small {
  display: flex;
  align-items: flex-start;
  gap: 8rpx;
  margin-bottom: 8rpx;
  font-size: 24rpx;
}

.impact-bullet {
  color: #999;
  flex-shrink: 0;
  margin-top: 2rpx;
}

.impact-detail {
  color: #666;
  flex: 1;
  line-height: 1.5;
}

.impact-recommendation {
  display: block;
  padding: 8rpx 12rpx;
  background-color: #FFF3E0;
  border-radius: 6rpx;
  color: #E65100;
  font-size: 24rpx;
  line-height: 1.5;
  border-left: 3rpx solid #FFA726;
}


